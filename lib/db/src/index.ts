// This production build uses the app's own local JSON database in
// artifacts/api-server/src/lib/local-db.ts.
// The schema exports are kept only for compatibility with old tooling.
export * from "./schema";
